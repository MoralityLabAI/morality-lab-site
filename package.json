{
  "name": "morality-lab-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix storyworld-app ci && npm --prefix storyworld-app run build && node scripts/build-site.mjs",
    "import:storyworlds": "node scripts/import-storyworlds.mjs"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
